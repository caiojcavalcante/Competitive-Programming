#include <bits/stdc++.h>

using namespace std;

vector <pair <int, int>> v;
set <int> solutions;

int main()
{
    int cards, card, k , l;
    cin >> cards;

    for(int i = 0; i < cards; i++)
        cin >> v[i].first;

    for(int i = 0; i < cards; i++)
        cin >> v[i].second;

    cin >> k >> l;

    for(int i = 0; i < k; i++)
    {
         
    }
}